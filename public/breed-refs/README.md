# Breed reference photos
Place side-profile photos of common breeds here. Filename pattern: `<lowercase-breed-name>.jpg` (e.g. `border-collie.jpg`). Used by future sprite-gen reference-image strategy. Currently unused — sprite gen is prompt-only.
